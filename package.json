{
    "name": "data-transform",
    "version": "1.0.0",
    "description": "A data transform example using TypeScript.",
    "main": "src/index.tsx",
    "author": "@joelmturner",
    "license": "MIT",
    "private": false,
    "scripts": {
        "build": "esbuild src/index.tsx --bundle --outfile=out/bundle.js --minify",
        "dev": "yarn build -- --servedir=out",
        "watch": "yarn build -- --watch"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    },
    "devDependencies": {
        "@types/react": "^18.0.26",
        "@types/react-dom": "^18.0.10",
        "esbuild": "^0.16.17"
    }
}